<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded-rect row. Pressed flashes a brighter orange (momentary touch
     feedback, distinct from the steady selected state); selected fills solid
     accent; idle is a barely-there fill + hairline. Pressed is listed first so
     a tap gives feedback even on the already-selected row. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/segment_idle" />
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/segment_idle" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_pressed" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/segment_idle" />
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/sheet_stroke" />
        </shape>
    </item>
</selector>
